
&НаКлиенте
Процедура Загрузить(Команда)
	ЗагрузитьДанные()
КонецПроцедуры  

&НаСервере
Процедура ЗагрузитьДанные()
	
	Лог.Очистить();
	
	ПараметрыВывода = Новый Массив;
	а_АппСелл.ЗагрузитьДанные(ПараметрыВывода,ПерезаписыватьДокументы);
	
	Для Каждого Строка из ПараметрыВывода Цикл
		Лог.ДобавитьСтроку(Строка);
	КонецЦикла;
	
КонецПроцедуры

	
